/*import Web3 from "web3";
import productArtifact from "../../build/contracts/product.json";
import shopArtifact from "../../build/contracts/shop.json";

const App = {
    web3: null,
    account: null,
    dhbw: null,

    start: async function () {
        const { web3 } = this;

        try {
            // get contract instance
            const networkId = await web3.eth.net.getId();
            //const deployedNetwork = 
        }
    }
}*/